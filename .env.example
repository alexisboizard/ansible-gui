SECRET_KEY=change-me-with-a-strong-random-key
DATABASE_URL=sqlite:///ansible_gui.db

# LDAP/AD and SMTP settings are now configured via the web UI (Settings tab).
# No need to set them here — they are stored in the database.
