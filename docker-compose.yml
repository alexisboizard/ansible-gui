services:
  ansible-gui:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/instance   # persists the SQLite database
    environment:
      - SECRET_KEY=${SECRET_KEY:-changeme-in-production}
    restart: unless-stopped
